import type { LayoutLoad } from './$types';

export const load: LayoutLoad = async ({ fetch, data }) => {
	// Auth state vine din +layout.server.ts în Faza 3
	// Deocamdată returnăm obiect gol
	return {};
};
